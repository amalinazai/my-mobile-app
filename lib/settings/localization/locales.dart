import 'package:flutter/material.dart';

class Locales {
  static const Locale enUS = Locale('en', 'US');

  // Add the locale to this list for any additional languages
  // that is added into the app

  /// List of supported locales to be passed into [lib/main.dart]
  static const List<Locale> supportedLangs = [
    enUS,
  ];
}
